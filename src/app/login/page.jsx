import Loginpage from '@/component/Login';
import React from 'react';

export const metadata={
    title:"Login Account"
}

const page = () => {
    return (
        <Loginpage></Loginpage>
    );
};

export default page;